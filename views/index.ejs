<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h1 > QOURA POSTS</h1>

    <% for(post of posts){ %>
        <div class="div">
     <h2 class="name">name :@<%= post.username %></h2>
     <h5>id : <%= post.id %></h5>
     <h4 class="content">content : <%= post.content %></h4>
     <div>
        <a href="http://localhost:8080/posts/<%=post.id%>"> see in detail</a>
        <a href="http://localhost:8080/posts/<%=post.id%>/edit"> edit post</a>

        <form method="post" action="/posts/<%=post.id%>?_method=DELETE">
        <button type="submit">Delete</button>
        </form>
     </div>
    </div>
    <% } %>
    <br>
    <br>
    <div class="adiv">
    <a class="newpost" href="http://localhost:8080/posts/new"> create new post</a>
</div>
</body>
</html>